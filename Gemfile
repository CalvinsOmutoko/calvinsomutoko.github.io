source "https://rubygems.org"

# GitHub Pages gem (this includes Jekyll as a dependency)
gem "github-pages", "~> 228", group: :jekyll_plugins

# Required for Ruby >= 3.0 to run Jekyll's server
gem "webrick", "~> 1.7"

# Optional: Add csv if you need CSV support
gem "csv"
gem "faraday-retry"